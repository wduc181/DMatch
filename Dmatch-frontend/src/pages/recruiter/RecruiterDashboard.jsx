const RecruiterDashboard = () => {
     return (
          <div>
               {/* TODO: Tổng quan (Số job đang hiển thị, số CV mới nhận) */}
               <h1>Dashboard Nhà Tuyển Dụng</h1>
          </div>
     );
};

export default RecruiterDashboard;
