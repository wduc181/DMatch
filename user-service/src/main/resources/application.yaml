server:
  port: 0   # random port

spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: default

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8111/eureka